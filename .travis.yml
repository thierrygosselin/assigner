# Sample .travis.yml for R projects

language: r
cache: packages
warnings_are_errors: false
sudo: required

notifications:
  email:
    on_success: change
    on_failure: change

before_install:
  - sudo apt-get update -q

apt_packages:
  - libgsl0-dev
