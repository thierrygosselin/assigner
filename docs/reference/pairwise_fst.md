# pairwise_fst

main function

## Usage

``` r
pairwise_fst(
  x,
  pop.pairwise = NULL,
  ci = FALSE,
  iteration.ci = 100,
  quantiles.ci = c(0.025, 0.975),
  digits = 9,
  path.folder = NULL,
  ...
)
```
