# pairwise_fst_nei

pairwise Fst function for nei

## Usage

``` r
pairwise_fst_nei(
  list.pair,
  pop.pairwise = NULL,
  data.genotyped = NULL,
  ci = FALSE,
  iteration.ci = 100,
  quantiles.ci = c(0.025, 0.975),
  digits = 9
)
```
