% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amova_genomic.R
\name{amova_genomic}
\alias{amova_genomic}
\title{Hierarchical AMOVA for incomplete genomic data}
\usage{
amova_genomic(
  data,
  hierarchy,
  strata = NULL,
  individual = "INDIVIDUALS",
  marker = "MARKERS",
  value = NULL,
  distance = c("euclidean", "identity", "nucleotide", "manhattan"),
  missing = c("locuswise", "filter", "complete"),
  min_call_rate = 0.8,
  min_groups = 2L,
  min_individuals = 2L,
  euclidean = c("check", "lingoes", "none"),
  standardized = identical(distance, "identity"),
  permutations = 0L,
  seed = NULL,
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{data}{A GDS file path or open GDS object accepted by
\code{\link[genometranslator:read_genome]{genometranslator::read_genome()}}. A long genomic data frame is also
accepted for programmatic use and testing.}

\item{hierarchy}{Character vector naming nested grouping columns, ordered
from highest to lowest level (for example \code{c("REGION", "POP_ID")}).}

\item{strata}{Optional strata file or data frame accepted by
\code{\link[genometranslator:read_strata]{genometranslator::read_strata()}}. It must contain \code{INDIVIDUALS} and the
columns named in \code{hierarchy}. When supplied, these hierarchy columns take
precedence over metadata stored in the GDS.}

\item{individual, marker}{Column names containing individual and marker IDs.}

\item{value}{Optional genotype-value column. With \code{value = NULL}, the
function derives diploid alternate-allele dosage from \code{ALT_DOSAGE}, numeric
\code{GT}, or six-digit calibrated \code{GT}. Supply a column name explicitly for
haplotypes, nucleotide sequences, or custom distances.}

\item{distance}{Molecular distance. \code{"identity"} is the 0/1 allelic or
haplotypic distance used for a Meirmans-style standardized statistic;
\code{"nucleotide"} is the proportion of nucleotide differences between
haplotype strings; \code{"euclidean"} uses squared dosage differences; and
\code{"manhattan"} uses absolute dosage differences. A function may also be
supplied; it must return a squared-distance matrix.}

\item{missing}{Missing-data strategy. \code{"locuswise"} uses all called
observations at each locus. \code{"filter"} first applies marker-level call-rate
thresholds and then works locus-wise. \code{"complete"} uses only individuals
called at every retained marker. No imputation is performed.}

\item{min_call_rate}{Minimum called proportion required in every lowest-level
group when \code{missing = "filter"}.}

\item{min_groups}{Minimum number of represented lowest-level groups required
at a locus.}

\item{min_individuals}{Minimum called individuals required per represented
lowest-level group.}

\item{euclidean}{How to handle a non-Euclidean squared-distance matrix.
\code{"check"} records the result, \code{"lingoes"} applies a Lingoes correction,
and \code{"none"} skips validation.}

\item{standardized}{Calculate a Meirmans-style standardized statistic. This
is available for \code{distance = "identity"}, where the theoretical maximum is
defined. It is deliberately not obtained by dividing distances by their
observed sample maximum.}

\item{permutations}{Number of hierarchy-aware randomizations. Zero disables
permutation tests.}

\item{seed}{Optional random seed used for permutations.}

\item{tolerance}{Numerical tolerance for Euclidean checks.}
}
\value{
An object of class \code{assigner_amova} containing global statistics,
locus variance components, missing-data diagnostics, and permutation
results when requested.
}
\description{
Computes a locus-wise analysis of molecular variance (AMOVA). Missing
genotypes are handled independently at each locus and all sample sizes,
degrees of freedom, and unequal-sample-size coefficients are recalculated
from the observations that actually contribute to that locus. This follows
the locus-wise strategy used by Stacks rather than deleting every individual
with at least one missing genotype.
}
\examples{
\dontrun{
# Hierarchy stored in GDS sample metadata:
fit <- amova_genomic(
  data = "genomic_data.gds",
  hierarchy = c("REGION", "POP_ID"),
  distance = "euclidean",
  missing = "locuswise",
  standardized = FALSE
)

# Hierarchy stored separately:
fit <- amova_genomic(
  data = "genomic_data.gds",
  strata = "amova_strata.tsv",
  hierarchy = c("REGION", "POP_ID")
)
}

}
\references{
Excoffier L, Smouse PE, Quattro JM (1992). Analysis of molecular
variance inferred from metric distances among DNA haplotypes. Genetics 131,
479-491.

Meirmans PG (2006). Using the AMOVA framework to estimate a standardized
genetic differentiation measure. Evolution 60, 2399-2402.
}
