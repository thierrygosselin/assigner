% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fst_WC84.R
\name{import_data}
\alias{import_data}
\title{Import and Standardize Genomic Data}
\usage{
import_data(data, calibrate.alleles = FALSE, verbose = FALSE)
}
\arguments{
\item{data}{Genomic dataset. Can be a tibble/data frame in wide format, a GDS file path, or a \code{SeqVarGDSClass} object.}

\item{calibrate.alleles}{Logical. If \code{TRUE}, force recalibration of REF/ALT allele genotypes using \code{\link[radiator]{calibrate_alleles}}. Default: \code{FALSE}.}

\item{verbose}{Logical. Show detailed messages during import. Default: \code{FALSE}.}
}
\value{
A tidy data frame (tibble) with standardized genotype format and required metadata (e.g., GT column).
}
\description{
Import and format genomic data into a tidy format compatible with downstream analyses in \pkg{radiator}. This function detects the input data type (tidy data frame, GDS file, or SeqVarGDSClass) and performs the appropriate transformation.
}
\examples{
\dontrun{
# From tidy data
tidy_data <- import_data(data = my_tibble)

# From GDS file
tidy_data <- import_data(data = "myfile.gds")

# With calibration
tidy_data <- import_data(data = my_tibble, calibrate.alleles = TRUE)
}
}
\seealso{
\code{\link[radiator]{detect_genomic_format}}, \code{\link[radiator]{tidy_wide}}, \code{\link[radiator]{calibrate_alleles}}, \code{\link[radiator]{read_rad}}, \code{\link[radiator]{gds2tidy}}
}
\keyword{internal}
